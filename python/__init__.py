"""Top-level package for small standalone Python utilities inside this repo.

This package exists mainly to support module-style CLIs like:

    python -m python.weather_forecast --city Beijing --days 2

Note: the package name is intentionally `python` to match the repo layout.
"""
